# radio_fi

A new Flutter project.

## Linux desktop audio playback

Audio playback on Linux desktop is provided via `just_audio_media_kit`, which relies on the
system-installed `libmpv` library. Before running the app on Linux, install it with:

```bash
sudo apt install libmpv-dev mpv
```

